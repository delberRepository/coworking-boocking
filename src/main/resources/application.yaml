spring:
  application:
    name: coworking-booking

  datasource:
    url: jdbc:postgresql://ep-plain-salad-algzr4df-pooler.c-3.eu-central-1.aws.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_P4ef9VtmBhuI # ¡RECUERDA CAMBIARLA POR SEGURIDAD!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true # Esto hace que el SQL en consola sea legible

  jackson: # Ya no lleva "spring:" delante porque ya estamos dentro del bloque spring
    serialization:
      indent-output: true